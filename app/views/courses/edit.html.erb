<h1 class="text-center">Update <%= @course.name %></h1>
<div class="col-md-6 col-md-offset-3">
  <%= simple_form_for @course do |f| %>
    <%= f.input :name %>
    <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>
</div>
